<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
 "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.quiz.lesson03.mapper.RealEstateMapper">
	<select id="selectRealEstateBy" parameterType="int" resultType="com.quiz.lesson03.domain.RealEstate">
		SELECT
			`id`,
			`realtorId`,
			`address`,
			`area`,
			`type`,
			`price`,
			`rentPrice`,
			`createdAt`,
			`updatedAt`
		FROM
			`real_estate`
		Where
			`id` = #{id}
	</select>
	<select id="selectRealEstateListByRentPrice" parameterType="int" resultType="com.quiz.lesson03.domain.RealEstate">
		SELECT
			`id`,
			`realtorId`,
			`address`,
			`area`,
			`type`,
			`price`,
			`rentPrice`,
			`createdAt`,
			`updatedAt`
		FROM
			`real_estate`
		Where
			`rentPrice` &lt; #{rentPrice}		
	</select>
	<!--  <select id="selectRealEstateAreaPrice" parameterType ="map" >
		SELECT
			`id`,
			`realtorId`,
			`address`,
			`area`,
			`type`,
			`price`,
			`rentPrice`,
			`createdAt`,
			`updatedAt`
		FROM
			`real_estate`
		Where
			`area` &gt; #{area, jdbcType=INT}
		AND	
			`price` &lt; #{price, jdbcType=INT}		
	</select> -->
</mapper>